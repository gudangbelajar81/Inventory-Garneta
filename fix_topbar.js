const fs = require('fs'); let html = fs.readFileSync('index.html', 'utf8'); html = html.replace(/el\("smart-search-container"\).style.display = "none";/g, ''); html = html.replace(/el\("page-title"\).textContent = label;\s*\}/g, 'el("page-title").textContent = label;\n      }\n      if (state.route === \'dashboard\' || state.route === \'ngitung\') {\n        el("smart-search-container").style.display = "";\n      } else {\n        el("smart-search-container").style.display = "none";\n      }'); fs.writeFileSync('index.html', html, 'utf8'); console.log('Fixed topbar');
